#include <stdio.h>
#include <conio.h>

int main()
{
    // sum
    int num1, num2, sum;
    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    sum = num1 + num2;
    printf("The Sum is: %d", sum);

    /* multiplication
    int a, b, multiply;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b
    multiply = a * b;
    printf("product = %d", multiply);
    */
    return 0;
}